{
  "name": "cf-demos",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.0.0",
    "npm": ">=11.0.0"
  },
  "scripts": {
    "update-env": "node scripts/update-env.mjs",
    "test": "node --test scripts/*.test.mjs"
  }
}
