import { DiagramGrid } from "../components/dashboard/DiagramGrid";

/** Authenticated dashboard view rendered at `/app` (and `/app/`): the diagram card grid. */
export function DashboardView() {
  return <DiagramGrid />;
}
